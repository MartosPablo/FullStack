const a = 4;
const b = "4";
if (a > b) {
console.log(`a (${a}) es mayor que b (${b})`);
document.write ("A es mayor que B")
}   else if (a < b)
{
    console.log(`b (${b}) es mayor que b (${a})`);
}   else if (a === b) {
console.log(`b (${b}) es igual y posee el mismo tipo que b (${a})`); 
}
else {
    console.log (`b (${b}) es igual en valor solo pero no en tipo`)
    document.write("Son otras condiciones")
}
